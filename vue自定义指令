,
    directives: {
      gzj: {
        bind: function (el, binding) {
        // 这里面访问不到this 不是箭头函数的问题 我们可以通过指令的值来传递进来 当前组件中的数据
          let flag = binding.value(el.textContent)
          if(flag != '-1') {
            el.style.color = '#f00'
          }
        }
      }
    }
